package HW2.designpatterns.facade;

public class Notes {
    /*
    -Provides a simplified interface to a library, a framework, or ny other complex set of classes

    -Downsides: may provide limited functionality

    -However, it includes only those features that clients really care about

    -Only useful when you need to integrate ur app w/ a sophisticated library that has dozens
    of features, but u only need a tiny piece of its functionality
     */
}
