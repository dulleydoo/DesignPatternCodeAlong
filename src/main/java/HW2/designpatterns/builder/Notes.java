package HW2.designpatterns.builder;

public class Notes {
    /*
    -Lets you construct complex objects step by step.

    -Allows you to use different types and representations
    of an object using the same representation code

    -Suggests that you extract the object construction code
    out of its own class and move it to separate objects called builders
     */
}
