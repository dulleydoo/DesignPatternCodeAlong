package HW2.designpatterns.adapter;

public class Notes {
    /*
    -Allows objects with incompatible interfaces to collaborate

    -It is a special object which converts the interface of one
    object so that another object can understand it
     */
}
